# Project 1 MNIST 分类

详细指标见 `codes/results/part_*_summary.txt` 与 `part_*_test_accuracy.txt`。  
详细的代码解释参考完整报告：[`report/PJ1报告.md`](report/PJ1报告.md)

**model files：** https://www.modelscope.cn/models/TOMOcaki/mnist-numpy-project1-25300740045  

- 在 `codes/` 下创建虚拟环境并安装依赖（见根目录 `requirements.txt` 或 `codes/requirements-freeze.txt`）。  
- 所有 `python` 命令均在 **`PJ1/codes`** 目录执行。


